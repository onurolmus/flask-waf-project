# Flask WAF Project

A modular web application security lab using Nginx, ModSecurity,
OWASP Core Rule Set, HTTPS, containers, and Ansible.

## Project Goal

Build an automated and reusable gateway that protects a Flask
application with a web application firewall.

